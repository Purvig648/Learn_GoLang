package mainandinit

import "fmt"

//main already declared

//Init

func Init() { //init does not have return type
	fmt.Println("Welcome Everyone Before we start off") //it gets executed before main
}
